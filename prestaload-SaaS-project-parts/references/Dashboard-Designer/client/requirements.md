## Packages
date-fns | Formatting dates for the dashboard header
recharts | Premium data visualization for all dashboard charts

## Notes
Static images: the application requires no external static images besides optional profile avatars (Unsplash).
Backend endpoints expected: 
- GET /api/dashboard/kpis
- GET /api/dashboard/sales-growth
- GET /api/dashboard/sales-vs-target
- GET /api/dashboard/product-stats
